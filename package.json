{
  "name": "syscoin-blocks",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "create": "graph create --node https://graph.pegasys.exchange/deploy pollum-io/syscoin-blocks",
    "deploy": "graph deploy --node https://graph.pegasys.exchange/deploy --ipfs https://ipfs.pegasys.exchange/ pollum-io/syscoin-blocks",
    "create-local": "graph remove --node https://graph.pegasys.exchange/deploy/ pollum-io/syscoin-blocks",
    "remove-local": "graph remove --node http://localhost:8020/ pollum-io/syscoin-blocks",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 pollum-io/syscoin-blocks"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.17.1",
    "@graphprotocol/graph-ts": "0.17.0"
  }
}